'use strict';
var $ = require('core-js/internals/export');
var hasOwn = require('core-js/internals/has-own-property');

// `Object.hasOwn` method
// https://tc39.es/ecma262/#sec-object.hasown
$({ target: 'Object', stat: true }, {
  hasOwn: hasOwn
});
