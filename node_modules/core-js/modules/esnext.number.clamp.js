'use strict';
var $ = require('core-js/internals/export');
var $clamp = require('core-js/internals/math-clamp');
var thisNumberValue = require('core-js/internals/this-number-value');

// `Number.prototype.clamp` method
// https://github.com/tc39/proposal-math-clamp
$({ target: 'Number', proto: true, forced: true }, {
  clamp: function clamp(min, max) {
    return $clamp(thisNumberValue(this), min, max);
  }
});
