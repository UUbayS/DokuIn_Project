'use strict';
var $ = require('core-js/internals/export');
var anObject = require('core-js/internals/an-object');
var AsyncFromSyncIterator = require('core-js/internals/async-from-sync-iterator');
var WrapAsyncIterator = require('core-js/internals/async-iterator-wrap');
var getIteratorDirect = require('core-js/internals/get-iterator-direct');

// `Iterator.prototype.toAsync` method
// https://github.com/tc39/proposal-async-iterator-helpers
$({ target: 'Iterator', proto: true, real: true, forced: true }, {
  toAsync: function toAsync() {
    return new WrapAsyncIterator(getIteratorDirect(new AsyncFromSyncIterator(getIteratorDirect(anObject(this)))));
  }
});
