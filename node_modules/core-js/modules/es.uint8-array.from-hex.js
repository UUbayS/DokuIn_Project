'use strict';
var $ = require('core-js/internals/export');
var globalThis = require('core-js/internals/global-this');
var aString = require('core-js/internals/a-string');
var $fromHex = require('core-js/internals/uint8-from-hex');

// `Uint8Array.fromHex` method
// https://github.com/tc39/proposal-arraybuffer-base64
if (globalThis.Uint8Array) $({ target: 'Uint8Array', stat: true }, {
  fromHex: function fromHex(string) {
    return $fromHex(aString(string)).bytes;
  }
});
